// Re-export from core service so all import paths resolve to the same singleton.
export { ToastService } from '../../core/services/toast.service';
export type { Toast } from '../../core/services/toast.service';
